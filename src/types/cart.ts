export interface CartItem {
  productId: string
  name: string
  slug: string
  image: string
  size: string
  color: string
  unitPrice: number
  originalPrice: number
  quantity: number
  maxQuantity: number
}
